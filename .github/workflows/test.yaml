name: Test

on:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/test.yaml"
      - "**/go.mod"
      - "**/go.sum"
      - "Taskfile.yaml"
      - "**.go"
      - "**.go.json"
  pull_request:
    branches:
      - master
    paths:
      - ".github/workflows/test.yaml"
      - "**/go.mod"
      - "**/go.sum"
      - "Taskfile.yaml"
      - "**.go"
      - "**.go.json"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go_version: ["~1.25", "~1.26", "~1.27"]

    steps:
      - uses: actions/checkout@v7

      - name: Install Go
        uses: actions/setup-go@v7
        with:
          go-version: "${{ matrix.go_version }}"
          cache-dependency-path: "**/*.sum"

      - run: go test '-coverpkg=github.com/trim21/go-phpserialize/...' -covermode=atomic -coverprofile=coverage.out -count=1 ./...
        working-directory: tests

      - uses: codecov/codecov-action@v7
        with:
          files: tests/coverage.out
          token: ${{ secrets.CODECOV_TOKEN }}

  test-checks: # referenced in Mergify conditions, must stay unique across all workflows
    needs: [test]
    runs-on: ubuntu-slim
    steps:
      - run: echo "all test checks passed"
